import { StyleSheet } from '@react-pdf/renderer'

export const textVariants = StyleSheet.create({
  body: {
    fontSize: 12,
    lineHeight: 1.5,
    color: '#4F4E5B',
    fontWeight: 'normal'
  },
  title: {
    fontSize: 32,
    lineHeight: 38
  }
})
